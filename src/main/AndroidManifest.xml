<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.weianyang.learningplatform">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".tool.LCPApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTask"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden|stateUnchanged"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.QuestionAddActivity"
            android:label="新增问题" />
        <activity
            android:name=".activity.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="登录" />
        <activity
            android:name=".activity.RegisterActivity"
            android:label="注册" />
        <activity
            android:name=".activity.AnswerBriefActivity"
            android:label="问题详情" />
        <activity
            android:name=".activity.AnswerDetailActivity"
            android:label="回复详情" />
        <activity
            android:name=".activity.AnswerAddActivity"
            android:label="添加评论" />
        <activity
            android:name=".activity.UserInfoActivity"
            android:label="用户详情" />
    </application>

</manifest>